class View3D
{
    void setup(){
      createCanvas(400,400, WEBGL);
    }
    
    void draw(){
        translate(width / 2, height / 2, 0);
        pushMatrix();
        box(150);
        popMatrix();
    }
}
